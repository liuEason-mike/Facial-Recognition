请检查后端接口：

POST /api/asr/diarization/align

当前请求返回：

{
  "code": 50000,
  "data": {
    "segments": []
  },
  "msg": "speaker_diarization_unavailable"
}

后端日志显示：

127.0.0.1 - - [25/May/2026 17:20:44] "POST /api/asr/diarization/align HTTP/1.1" 503 -

需要判断当前问题到底是：

前端没有成功发送音频文件；
后端 /api/asr/diarization/align 没有正确接收到音频；
后端接收到音频，但没有成功调用 pyannote 服务；
pyannote 服务不可用或请求失败。