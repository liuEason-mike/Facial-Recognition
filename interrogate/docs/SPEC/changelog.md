# 需求变更记录

| 日期 | 版本 | 变更内容 | 来源 |
| --- | --- | --- | --- |
| 2026-04-22 | v0.1 | 基于当前代码生成项目工程 spec 初版 | 代码读取 |
| 2026-04-22 | v0.2 | 新增运维部署规范，覆盖部署拓扑、目录、环境变量、compose、Nginx、发布、回滚、备份、监控和排障 | 用户要求 |
| 2026-04-22 | v0.2 | 明确 PRD 与 spec 的协作方式：PRD 定义产品目标，spec 定义工程约束 | 用户要求 |
| 2026-04-25 | v0.3 | 统一文档结构为 `docs/SPEC/` + `docs/api/`，明确 `html-next/` 为本地新前端入口，统一 WebSocket 服务端口为 `5000` | 用户要求 |
| 2026-05-19 | v0.4 | `html-interrogate` 前端 ASR WebSocket 改为直连 `ws://8.140.17.203:6196/ws/asr`，不再经由后端 `/ws/asr` 代理 | 用户要求 |
| 2026-05-21 | v0.5 | 调整 `/ws/face` 基线模型状态语义：`test_status=0` 采集并异步训练基线，`test_status=1` 停止基线训练，`test_status=2` 使用 ready 基线评分；该语义已被 v0.7 覆盖 | 用户要求 |
| 2026-05-21 | v0.6 | 明确 `/ws/face` 的 `test_status=1` 只封板并快速返回，不再同步异常评分和写入异常表；该语义已被 v0.7 覆盖 | 用户反馈 |
| 2026-05-21 | v0.7 | 重新调整 `/ws/face` 状态语义：`test_status=1` 基准结束后持续异常评分但暂不写 `emotion_anomaly`，`test_status=2` 作为审讯结束释放内存模型缓存信号 | 用户要求 |
| 2026-05-21 | v0.8 | 明确 `ready` 或 `released` 模型状态不得影响 `/ws/face` 基础实时检测展示数据返回，`released` 只影响异常评分模型和 `anomaly_data` | 用户要求 |
| 2026-05-22 | v0.9 | 新增 ASR 关键词提炼接口配置：默认使用 DashScope OpenAI-compatible 地址、`DASHSCOPE_API_KEY` 和 `qwen3-max`，并保留规则兜底 | 用户要求 |
| 2026-05-22 | v1.0 | 修复 ASR 关键词大模型异常穿透导致 `/api/asr/keywords/extract` 返回 500 的问题，并将前端累计触发阈值从 300 字调整为 100 字 | 用户反馈 |
| 2026-05-22 | v1.1 | 按 `2026052203html.md` 调整 `html-interrogate`：音频采集图标旁显示状态文字、视频分析叠加层支持一键隐藏/恢复、历史记录支持折叠并展示两条带头像记录 | 用户要求 |
| 2026-05-22 | v1.2 | 按 `2026052204html.md` 将右侧历史记录模块替换为内存态人脸头像时间线：视频每秒采样、每分钟刷新展示并释放上一轮数据 | 用户要求 |
| 2026-05-22 | v1.3 | 按 `2026052202-asr.md` 将后端 `/ws/asr` 代理改接 DashScope WebSocket ASR，并将识别回包归一化为 `[{start,end,text}]` 片段格式 | 用户要求 |
| 2026-05-25 | v1.4 | 排查 `/ws/face` 微表情回包慢的问题：新增 `client_seq` 回显、`processing_ms/performance` 分段耗时、慢帧日志阈值，并在 `html-interrogate` 端增加视频帧 in-flight 背压；微表情面板将 `angry/愤怒` 展示为 `值` | 用户要求 |
| 2026-05-25 | v1.5 | 按 `2026052501.md` 继续调整：移除 `html-interrogate` 微表情列表中 `angry` 的可见“愤怒”字样；`/ws/face` 在面部分析空结果或异常时返回固定零值 `emotion_scores`；新增 `face_landmark_ms`、`emotion_ms` 耗时拆分和 `FACE_EMOTION_INFER_EVERY_N_FRAMES` 情绪推理缓存间隔 | 用户要求 |
| 2026-05-25 | v1.6 | 按 `2026052502.md` 调整 `html-interrogate` 微表情展示：标题右侧不再显示主导情绪文字，`愤怒` 回到左侧情绪列表；当前端收到 `/ws/face` 七项全 0 `emotion_scores` 时，在展示适配层使用每 15 秒切换一次的演示分布 | 用户要求 |
| 2026-05-25 | v1.7 | 规范化 `2026052503.md` 说话人分离需求文档：明确 pyannote 外部服务调用、ASR 文本对齐、前端“说话人转写”展示、错误降级、影响范围和验收标准 | 用户要求 |
| 2026-05-25 | v1.8 | 按 `html-interrogate-ui-adjustments/2026052501.md` 调整前端讯问室：音频波形可视化从右侧栏移到左侧栏底部，并改为 macOS 音频编辑器质感、审讯室主题色的横向线条波形 | 用户要求 |
| 2026-05-25 | v1.9 | 规范化 `html-interrogate-ui-adjustments/2026052502.md` 关键词详情浮框需求：明确四类可点击关键词、右侧浮框交互、固定模拟数据、样式要求、影响范围和验收标准 | 用户要求 |
| 2026-05-25 | v2.0 | 按 `html-interrogate-ui-adjustments/2026052502.md` 实现 `html-interrogate` 关键词详情浮框：四类关键词可点击，右侧浮框展示固定模拟信息，支持遮罩、关闭按钮和 `Esc` 关闭 | 用户要求 |
| 2026-05-25 | v2.1 | 修复关键词详情浮框触发判断：当 ASR 将“身份证、手机号、车牌号、车次”提炼为普通分类但关键词文本或来源命中时，前端仍渲染为可点击标签并打开对应详情 | 用户反馈 |
| 2026-05-25 | v2.2 | 按 `html-interrogate-ui-adjustments/2026052503.md` 优化关键词详情右侧抽屉：四类关键词分别展示独立核验/查询页面，补充状态、数据来源、查询编号和真实系统查询字段 | 用户要求 |
| 2026-05-25 | v2.3 | 按 `html-interrogate-ui-adjustments/2026052501.md` 补充音频波形播放指针：左侧波形面板在 ASR 采集中显示蓝色竖向指针，并随实时音频包进度横向移动 | 用户要求 |
| 2026-05-25 | v2.4 | 按更新后的 `html-interrogate-ui-adjustments/2026052501.md` 移除音频波形面板蓝色竖向播放指针，保留横向 macOS 风格主题波形展示 | 用户要求 |
| 2026-05-25 | v2.5 | 按 `update-backend-adjustments/2026052503.md` 实现 ASR 说话人分离旁路链路：后端新增 pyannote multipart 调用与 ASR 时间片对齐接口，前端新增 15 秒 WAV 窗口上传和“说话人转写”展示 | 用户要求 |
| 2026-05-25 | v2.6 | 修正 `html-interrogate` 实时 ASR 主链路：前端重新连接项目后端 `/ws/asr`，由后端代理 DashScope 公有实时 ASR 模型，不再直连 `ws://8.140.17.203:6196/ws/asr` | 用户要求 |
| 2026-05-26 | v2.7 | 重构前后端一体 Dockerfile：Node 阶段构建 `html-interrogate`，Python 阶段通过 `run.py` 同端口托管前端、HTTP API 和 WebSocket，并新增 `.dockerignore` 排除本地密钥、缓存和构建产物 | 用户要求 |
| 2026-05-26 | v2.8 | `/ws/asr` 增加 DashScope 代码内置默认密钥以适配后续 Docker 运行；转发失败时向前端返回 `asr_forward_failed`，前端断开时同步关闭上游 ASR 连接 | 用户要求 |
| 2026-05-26 | v2.9 | 优化前后端一体 Dockerfile 缓存层顺序：前端依赖与源码拆层，后端最终镜像只复制运行必需文件，减少普通代码提交后的重复依赖构建等待 | 用户要求 |
| 2026-05-26 | v3.0 | 按 `html-interrogate-ui-adjustments/2026052601.md` 调整关键词提炼交互：关键词展示具体 label，点击后右侧短抽屉先展示专业查询中页面，再切换为银行卡、手机号、车牌号对应查询结果页 | 用户要求 |
| 2026-05-26 | v3.1 | 按 `html-interrogate-ui-adjustments/2026052602.md` 调整关键词查询右侧浮框：结果页改为无滚动紧凑网格，将状态、来源、查询编号、时间、字段和核验结果重新排列到单屏展示 | 用户要求 |
| 2026-05-26 | v3.2 | 增加 `ENABLE_DATABASE` 后端开关：默认跳过 SQLAlchemy 初始化、自动建表和实时入库，避免未准备数据库时容器启动失败；启用数据库时仍按 MySQL 配置运行 | 用户反馈 |
