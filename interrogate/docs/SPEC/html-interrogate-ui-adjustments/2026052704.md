1. 左侧栏案情摘要默认是收起，
   当收起的时候：- 将“状态检测”模块适当拉高；
   - 让左侧整体模块高度撑满左侧区域；
   - 保持整体布局不变，只优化高度分配。
   - 将“状态检测”模块高度提高到撑满左侧剩余空间
   - 让该模块自动填充左侧空白区域；

2.将页面中的 au52 或 AU52 数值/标签字号稍微调大。
AU52 不需要过度突出，但需要比普通文本更容易识别。
给 AU52 增加一个半透明黑色背景框：
背景颜色使用半透明黑色；
圆角处理；
内边距适中；
不遮挡视频或其他关键内容；
保持整体视觉风格一致。
